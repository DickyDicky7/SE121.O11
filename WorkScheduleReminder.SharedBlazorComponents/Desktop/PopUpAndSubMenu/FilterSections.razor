<MudPaper @attributes=AdditionalAttributes
          Class="d-flex flex-column gap-2 pa-2"
          Style="border-radius:20px;overflow:scroll"
          Elevation="0"
          Width="250px"
          Height="500px">
    @foreach (SectionChecked sectionChecked in SectionFilterList)
    {
        <MudPaper Class="d-flex flex-row align-center justify-space-between"
                  Elevation="0"
                  Width="100%">
            <MudText Style="margin:4px">
                @sectionChecked.Name
            </MudText>
            <MudCheckBox @bind-Checked=sectionChecked.Checked
                         CheckedIcon="@Icons.Material.Filled.Check"
                         UncheckedIcon="@Icons.Material.Filled.Close"
                         Size="Size.Small"
                         Color="Color.Info"
                         UnCheckedColor="Color.Error"
                         Dense="true" DisableRipple="true" />
        </MudPaper>
    }
</MudPaper>

@code {
    [Parameter(CaptureUnmatchedValues = true)]
    public IDictionary<string, object>? AdditionalAttributes { get; set; }

    public record SectionChecked()
    {
        public string Name { get; set; } = string.Empty;
        public bool Checked { get; set; } = false;
    }
    public List<SectionChecked> SectionFilterList = new List<SectionChecked>();

    protected override void OnInitialized()
    {
        base.OnInitialized();
        SectionFilterList.Add(new SectionChecked() { Name = "hihi" });
        SectionFilterList.Add(new SectionChecked() { Name = "hehe" });
        SectionFilterList.Add(new SectionChecked() { Name = "haha" });
        SectionFilterList.Add(new SectionChecked() { Name = "huhu" });
        SectionFilterList.Add(new SectionChecked() { Name = "hihi" });
        SectionFilterList.Add(new SectionChecked() { Name = "hehe" });
        SectionFilterList.Add(new SectionChecked() { Name = "haha" });
        SectionFilterList.Add(new SectionChecked() { Name = "huhu" });
        SectionFilterList.Add(new SectionChecked() { Name = "hihi" });
        SectionFilterList.Add(new SectionChecked() { Name = "hehe" });
        SectionFilterList.Add(new SectionChecked() { Name = "haha" });
        SectionFilterList.Add(new SectionChecked() { Name = "huhu" });
        SectionFilterList.Add(new SectionChecked() { Name = "hihi" });
        SectionFilterList.Add(new SectionChecked() { Name = "hehe" });
        SectionFilterList.Add(new SectionChecked() { Name = "haha" });
        SectionFilterList.Add(new SectionChecked() { Name = "huhu" });
    }
}
